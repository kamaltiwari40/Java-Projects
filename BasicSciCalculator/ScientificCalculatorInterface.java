package BasicSciCalculator;

interface ScientificCalculatorInterface extends Calculator {
    double power(double a, double b);

    double square(double a);

    double squareRoot(double a);
}
